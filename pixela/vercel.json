{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || echo \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -qF '[deploy]'; then exit 1; else exit 0; fi",
  "github": {
    "silent": true
  }
}
